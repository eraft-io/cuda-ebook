\section{性能分析方法论}

大家好，在今天的文章中，我将对上一节最后一个应用程序进行一些性能分析。我们需要做的只是改变块大小（BlockSize），
看看它如何影响不同的性能指标。让我们从 CUDA 代码开始，如我们所见，这里有一个将两个向量相加并将结果存储在第三个向量
中的内核，该向量称为 C 向量。然后在主函数中，我们遵循一般 CUDA 应用程序的步骤。正如我们在这里看到的，我们定义了
一些指针，然后在 GPU 中为这些向量或指针分配空间，然后我们在 CPU 端做同样的事情，并初始化向量 A 和B，然后我
们将数据从 CPU 中的 A 和 B 复制到 GPU 中的 A 和 B，以便能够在 GPU中执行加法操作。然后我们执行内
核来执行加法操作，最后我们使用这个 \texttt{cudaMemcpy} API返回结果。这就是我们在这个应用程序中需
要做的和需要了解的全部内容。

大家好，在今天的文章中，我将对上一节最后一个应用程序进行一些性能分析。我们需要做的只是改变块大小，看看它如何影响不同的性
能指标。让我们从 CUDA 代码开始，正如我们在这里看到的这段代码始于 GPU 内核，它将向量 A 加到向量 B 上，并
将结果存储在向量 C 中。然后在主函数内部，我们这里有典型 CUDA 应用程序的一般步骤。如果你还记得，我们定义一些变量
，然后在 GPU 内存中为这些变量分配空间。这里我们正在初始化向量 A 和 B，我们在 CPU 端或CPU 内存中做同样
的事情。这里我们将初始化好的 A 和 B 的数据发送到 GPU 内存，以便能够使用这里的内核即 GPU 内核来执行。最后
一步是使用这里的这个 API，将数据或结果从 GPU 内存复制回 CPU 内存。这里的最后一件事就是释放我们在这个应用程
序中已经使用的所有内存空间。这里我们需要做的是控制或操作这个变量。

看到了吗？每个网格的块大小，实际上我们有两个变量，不是一个。因为如果你记得我曾说过，对于任何 CUDA 内核，我们有两个
配置。第一个是每个块的线程数，第二个是应用程序中的块数。如果我们搜索这个，你可以往上翻，看到我们正在从终端读取这个值。当
我们执行这个应用程序时，我们需要在执行时添加块大小，并且这个值将被输入到这里的这条指令中。相比于我们在上一节讨论的内容，
这只是我们在本应用程序中所做的唯一更改。

我们回到这里，这个文件叫 \texttt{test\_01}。这里我有一个名为 Section 05 的新章节，我们输
入 \texttt{ls} 命令，这是我们的文件。我们需要编译这个文件，我们写 \texttt{nvcc-o}，然后我们
需要为可执行输出起个名字。我们可以说例如 \texttt{test\_01}，你可以就这样写，或者你也可以
写\texttt {.exe}，两者都可以好吗？然后我们需要 CUDA 应用程序的 \texttt{.cu}文件，接着我
们现在只需要按回车键。我们这里有可执行文件，我们可以直接复制这个并按回车键。

我们需要做的就是来到这里写一个空格，然后假设块大小是 256 个线程。如你所见，它没有运行，因为让我们先去掉这个空格，它
还是不行。因为它在这里要求块大小，对吗？然后按回车，应用程序成功执行，没有任何问题。我知道终端上不会显示任何内容，因为我
们没有打印任何东西。总之这就是我们需要做的。你可以在这里放一个\texttt{printf} 或类似的东西，以确保你的执
行成功完成。

\subsection{Python自动化分析脚本}

我们需要改变块大小，并用不同的块大小来分析这个应用程序，以观察这个变化的影响。这里我有一个 Python 文件，它可以完
成所有这些分析。我将读取一些性能指标，然后我们有 L1 命中率、L2命中率、占用率（Occupancy）。第一个是执行时
间，第二个是活动周期数（ActiveCycles）。因为通常你可以用秒或周期数来衡量时间。好的。这是我们用来读取占用率的
指标。这里我们测量一个叫做分支效率（Branch Efficiency）的东西，我们将在关于分支发散（BranchDivergence）的章节中讨论它。你今天不需要关注那个，但我只是想让你更容易地提前开始了解。这个叫做分支效率。


最后我们有 DRAM 吞吐量。你知道 DRAM吞吐量是通过内存每秒访问的千兆字节数（GB/s）来衡量的。我们需要知道这个
应用程序的执行时间是多少秒，并使用该值来计算 DRAM 吞吐量。时间会影响 DRAM吞吐量，因为吞吐量是通过每秒千兆字节
数来衡量的。工具会帮我们做这件事，但我只是在解释所有东西，这些就是指标。我知道我把它们写了两次，一次在这里，另一次在这里
，但每个都有其用途，这个将和NCU 一起使用。如果你记得我们可以来到这里，像这样写 \texttt{ncu}，然后两个短
横线\texttt{--metrics}，就像我们在上一节讨论的那样。

然后你可以在这里列出所有这些指标，如你所见，我们得到了所有值。但我在这里只是使用 Python文件来为不同的块大小做这个
，而不仅仅是一个。让我们复制所有这些，把它们夹在这里，然后点击回车。我不想看着终端显示很多表格，然后把这些值复制到Excel 表格中再进行比较。我需要 Python 脚本来完成所有这些。这就是为什么我这里准备了 Python脚本。我们可以用

这个，但你知道我需要它们作为一个数组。就像你在这里看到的，我使用这里的这个变量——header变量——来为我们从这个 Python 脚本输出的 CSV 文件创建一个表头。然后这里我们打开输出文件，以便在运行 NCU时存储这些指标名称及其值。


然后对于每个块大小，我从 32 开始，因为最小的块大小是一个 Warp。我们从 32 个线程开始，一直到1024。我们知
道一个 Warp 由 32 个线程组成，对吗？并且我们取最大值1024，因为根据白皮书，我们不能使用超过这个数字的块大小
。

如果你还记得在硬件章节，我想是第二节或第一节。我告诉过你，根据白皮书，我们不能使用超过 1024个线程的块大小。然后这里
我们创建我们的命令，我知道这里是 1024。因为我们的范围在这里，所以我们只需要在 1024上加 1，以确保在 for
循环中包含 1024 这个值。

这是我们的命令，如果你看这里，它和这里的命令完全一样，我们将使用这个变量，而不是写出所有指标。但我没有尝试这些指标，因为
我一开始已经在这里写了，它们如你所见。如你所见，我在这里使用这个变量，然后这是我们的可执行文件。这里我们添加块大小，块大
小如我们所见，范围从32 到 1024，步长为 32。即是 32、64、96、128，以此类推，每一步我们增加一个 Warp 或增加 32个线程。


然后这里这是一条 Python 指令，用于运行这个命令。这完全等同于执行这里的这个即按回车执行这个命令。这就是我们在Python 编程中执行终端命令的方式。这只是一个 \texttt{print}，然后这只是一个\texttt{print}

，用于显示我们当前正在为这个块大小运行的信息。这意味着这里的输出或这里的表格将被存储在result 变量中。然后当我们执
行这个 NCU 命令时，我们获取输出并将其存储在 result变量中。我们现在需要做的就是从这个输出中读取指标名称及其值
，并将它们全部存储在一个文件中。

我要说我们需要分割这个 result 变量。把它们全部存储在这里的输出文件中，在这个 CSV文件中分成不同的行。然后我将
遍历我们拥有的所有行，我们将逐行读取。对于每一行我们将查看或检查指标名称。一旦我们有了一个指标名称，我们就尝试读取它的值
。

一旦我们有了值，我们需要将这个值存储到输出文件中。这里有很多细节，这些细节只是为了去除所有我们不需要的东西。因为如果你看
一下输出，例如我们不需要所有这些，对吧？我们不需要所有这些，我们只需要指标名称和它的值。如果我们这样做，你会发现它比这个
表格更奇怪。

顺便说一下，如果你重复运行，因为我在这里使用两个短横线 \texttt{--csv} 来确保它们以 CSV文件格式显示，
而不是表格。看到它是什么样子了吗？我们需要去掉这个逗号，需要去掉这个双引号等等。记住，所有这些只是为了去除我们不需要的任
何东西，或者只是从输出中移除它。

我们需要做的，从这个文件中我们需要的就是这两样东西：指标名称和它的值。然后我们这里需要的是获取指标值，并将其存储在输出文
件中。一旦我们在输出文件中有了所有内容，我只想提一下我们具体存储在哪里。

是的，打开输出文件，我们从这里开始写表头，我们需要看到写入表头。看到表头了吗？因为表头应包含块大小以及每个指标的名称。是
的，这里我们尝试获取指标值，然后将其写入输出文件。然后我们应该看到另一个写入指令来写入值，然后这里我们需要绘制每个指标随
块大小变化的关系图，意思是我们将块大小从32 改到 1024 对吧？与例如周期数的关系等等。我们需要绘制所有这些，这就是
我们在这里做的。

我们需要例如——我们需要在图表中显示块大小与占用率的关系，与时间的关系，与执行时间的关系，块大小增加。

\subsection{分析结果图表}

让我们运行，这就是我们全部的内容。我们在纵轴上绘制所有指标，在 X 轴上应该是块大小。想象一下从 32 到 1024步长
为 32，这大概有 15 次左右。

我将添加这个 Python脚本，以便你不仅可以将它用于这个应用程序。因为我只是给你一个如何分析许多配置的想法，只需一次运
行。因为想象一下我们在这里运行了多少次。

让你来到这里使用这个命令 15次，然后手动读取这些值并把它们放进文件，这是非常困难的，这将会花费太多时间。而且如果你想用
不同的配置再次重复它也会非常累人。这就是Python 文件的样子，以及我们这里需要做的所有事情。

我们需要做的就是来到这里输入 \texttt{python3}，文件名是\texttt{get\_insight.py}
，然后按回车。

它将为所有步骤或所有迭代重复运行。现在我们正在为块大小 32 运行。我们可以到这里看到这个 \texttt{print}
通过这条指令体现出来。这就是我们从这个脚本中需要的全部。一旦它完成了所有迭代，脚本将给我们一些图表。除此之外，我想再看另
一个文件，我们需要查看图表，看看块大小的变化如何影响不同的性能指标。现在我们完成了，我们输入\texttt{ls}，但我
不想让你困惑，所以让我们等等看从这个输出中能得到什么。看看这些图表里的内容。

如你所见，我们有一些图表，所以我们想到这里，在第一个图表里我有 GPU时间、执行时间和周期数。让我们看看第一步或第一次迭
代，其中块大小是 32 个线程或一个Warp。如你所见，对于两者——时间和活动周期——与其他所有迭代相比，我们都有更大的
值。当我们每个块使用一个 Warp时，我们得到了更长的执行时间。如你在此所见，我知道其他所有迭代中，这里只是稍微增加了一
点，但大多数几乎相等。我们需要拿这个值和其他值进行比较，和其中任何一个值比较。这意味着我们需要进行一些性能分析，以理解为
什么我们这里的执行时间比其他迭代长。

让我们看看这如何影响其他指标。这里有 L1 缓存和 L2 缓存。对于 L1我们看到对于所有迭代它都是零。好的。因为改变块
大小并不会改变我们访问全局内存的方式，我们将访问相同的扇区（Sector）或相同的字节。这就是为什么L1 缓存是恒定的且
等于0，但我们在这个应用程序中没有那样做。如果你改变例如步长（Stride），即每两个元素之间的步长，这可能会改变。

好的，这里 L2 大约等于 33点几，它略有变化或变化很小，但几乎相同。在这个图表里我们没有看到任何变化。再次我只是想告
诉你，这里我们有块大小，这里是指标值，L1命中率和 L2 命中率。因为如果你看内核时间，你可以看到第一个值很大，而所有其
他值都很小。

让我们看最后一个，如你所见，内存带宽与内核时间成反比关系。对于内存带宽和 Warp 活动度或 Warp占用率，你可以在这
里看到相反的情况。你可以看到第一个值很小，但所有其他值都很高。那么让我们开始做吧，这就是为什么我们需要关注第一个值，并拿
例如第二个值对两者进行一些性能分析。让我们来到这里说，例如我们需要输入\texttt{ncu}，我们需要的
是 \texttt{./test.exe}，然后我们输入 32。

我需要看看当我们块大小是 32 个线程时，不同的部分（Section）是什么样的。网格大小（GridSize）指的是这个
应用程序中所有的块。这些是部分，我需要看的是块大小，如我们所见是32，后是网格大小。这是一个很大的数字，我知道这是一个很
大的数字，因为我们有数百万个元素需要相加。如果你再看一下这里的应用程序，你可以看到这些向量中每一个的大小是这个值，这是一
个非常大的值。以及块大小，正如我们所说是 32个线程。如果我们有这个数量的元素，这是一个很大的数字，而我们只有 32个线
程。如果我们用这个除以这个就得到块数，这就是你得到块数的方法。

总之对于每一个网格的块数，我们通常使用这个公式：总大小加每个块的线程数减一，除以每个块的线程数。而这个应用程序结合我们给
每个块的线程数的值，两者共同给出了我们在这个应用程序中需要的线程总数。这给了我们每个网格正确的块数。因为记住，你想确保应
用程序中的线程总数等于你要相加的元素总数。

总之，我们这里需要做的是取这个值和这个值，并把它们放在这张幻灯片里。如我们所见，每当我们在本应用程序中使用块大小 32时
，总块数将是这里的这个值。那么我们在这个应用程序中有多少个活动块（Active Blocks）呢？

如果你还记得我在上一节解释了“已分配活动块”（Allocated ActiveBlocks）这个术语，并且我说过这真的非
常重要。在继续这个文章之前，你需要先看那个文章。总之我们具体在哪里可以找到这个值？我们有16、128、48，你可以来到这
里进入占用率部分，然后取这四个值中最小的那个。已分配活动块的数量将是16。这就是为什么我这里加了 16。很好，使用 CUDA API，你可以获取 GPU 上可用的SM（流多处理器）数量，对吧？在你的 GPU 上，我得到的数量是 38 个 S

M，38 个流多处理器。

\subsection{波次与执行时间分析}

现在我想计算波次（Wave）数量，这里我们需要了解更多关于波次定义的信息。我告诉过你，已分配活动块的数量通常指的是你可以
在一个SM 中同时运行的块的数量。意思是在那种情况下，我们可以同时运行 16 个块在每个 SM 上。但是记住块的总数不是
16、32 甚至 64，而是超过 100 万。从那个巨大的数字中，我们可以在每个 SM 上同时运行 16个，那么剩下的块
怎么办？这就是我们所说的波次。

让我们例如除以这个值，我们将这个值也就是总块数除以 16。我告诉过我们可以同时运行 16 个，对吧？然后我们除以 SM的
数量，因为我们讨论的是一个流多处理器。这就是我们在这个应用程序中，每个 SM 将要运行的波次数量。

再重复一遍，当这个应用程序启动时，它从这个数字中取出 16 个块交给第一个SM，意思是对于这里的每个时间点，每个波次，我
们将在每个 SM 上运行 16 个块。同样的情况也适用于所有其他SM。一旦这些 SM 中的任何一个完成了它的 16个块，
就意味着它完成了第一个波次。那么它会请求另一个波次，它会请求另外 16 个块。

情况就是这样，让我们看一下这里，这是我们的 SM，这是我们的第一个波次。如你所见，我们有 16 个块 ID，从 0 到1
5。一旦我们完成了所有这些块，SM 就可以获取另一个波次，可以获取另外 16个块。但是再次当我们执行第一个波次时，我们不
能获取这些块中的任何一个。即使所有这些块都因依赖而停顿，因内存操作而停顿或因任何其他原因停顿，你同时只能有16 个块在每
个 SM 上。一旦你完成那个波次，你可以再获取 16 个块，就是这样。

这个术语非常重要，我将解释它是如何重要的。总之这是我们使用块大小 32 的情况。如果我们使用块大小64，这些是数字，我又
运行了 NCU来获取总块数，这是我们的总块数。为什么我们这里的总块数是那个数字的一半？这很简单，如果你增加每个块的线程数
，你就必须减少总块数。因为我告诉过你总块数结合每个块的大小，每个块的大小应该等于你应用程序中元素的总数。

这就是它的工作原理。现在我们需要知道我们有多少个波次。再次我只是想向你展示第一种情况的波次数量，让我们看这里，看到这个数
字了吗？每个SM 的波次数量，你可以从这里检查它，这完全是同一个数字。

让我们回到这里除以这个值，这是总块数。应用程序中的所有块，所有这些块都应该在 38 个 SM 上执行。我们需要知道每个SM 将执行多少个块，这就是我们需要做的。我要复制这个值并除以 38。那样的话，每个 SM 应该执行超过 13000个块。


但我们不能同时执行所有这些块，我们可以在不同的波次中调度所有这些块。每个波次由 16 个块组成，我们需要用 16除这个数
以得到每个 SM 的波次数量。如你所见是 862，如果你在这里运行 NCU，你会发现完全相同的数字。如你所见是862。

我只是想在这里提一下波次数量，因为你需要很好地理解它。总之让我们回到第二张幻灯片，因为它非常重要。我告诉过你这种情况的执
行时间比第二种情况的执行时间长。你还记得在这里吗？不在这里，是在这个图表里。让我们回头看看，你还记得执行时间或活动周期数
吗？对于块大小32，它比块大小 64 的大，这没关系。

那么我们需要理解为什么情况是这样的。

\subsection{块大小对性能的影响}

想象我们有一个 GPU 和 2 个 GPU——抱歉，两者都在执行同一个应用程序，但使用不同的块大小。想象我们将只分析38
个 SM 中的一个 SM。在这种情况下我们有一个 SM，在这种情况下我们也有这个SM，但区别在于——并且在两种情况下我们
都有 16个已分配活动块。让我们确认一下。是的，如果你看这里的占用率部分，你可以看到这四个值的最小值是16。在两种情况下
已分配活动块的数量都是 16。这两条线代表一个 Warp，这就是为什么我这里有 16 个从 0 到15，这里也是从 0
到 15 对吗？

这里我们有两个 Warp，为什么？因为这个块由 64 个线程组成，块大小是 64，而一个 Warp 等于 32个线程，这
就是为什么这个块比这个大。如果我们用 32 除这个，那么我们每个块就有两个 Warp。

想象有一个内存操作（红色的那个）以及一个依赖于该内存操作的加法操作。在这种情况下，每个块将执行内存操作，并会等待内存操作
完成。而内存操作通常比GPU 中的算术操作花费更长时间。如果我们在 L1 缓存命中它可能花费 30 个周期，或者如果在
L2 缓存命中大约200 个周期，或者如果在全局内存命中 300 或 400 个周期。但这里我们有依赖关系，一旦我们因这
个依赖而停顿，SM将不会执行任何操作。

所有 Warp 或所有块都会停滞，对吗？但这里我们有不同的 Warp。如果这里的第一个 Warp 因任何原因停顿，SM可
以获取第二个 Warp 并执行它。我们可以在不同的 Warp 之间进行调度，以确保最小化 SM内发生的停顿次数。我希望这
对你来说很容易理解，但我只是想为你做一个简单的分析作为开始。这就是为什么当我们块大小是 64时，总执行时间比块大小是 3
2时要短。不过我们将在接下来的章节中进行更多的性能分析。今天的内容就到这里，我们下个文章再见。


